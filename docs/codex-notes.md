# Codex notes

## 2026-06-18

- Analysis and chat must share the analysis job ID; do not reintroduce independent demo chat routes.
- Preview behavior must stay explicitly labelled. Production API failure must be visible to the user.
- Repository chat claims require file and line references from the cloned snapshot.
- Keep model selection under server policy by default; expose overrides only when the backend supports them.
- Preserve `feat/advanced-chat-ui` as the pre-integration checkpoint branch.
