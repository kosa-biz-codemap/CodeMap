fastapi>=0.100.0
uvicorn[standard]>=0.23.0
sqlalchemy>=2.0.0
pydantic[email]>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
python-multipart>=0.0.9
alembic>=1.11.0
asyncpg>=0.28.0
websockets>=11.0.0
httpx>=0.27.0

# [Sec09 - LangGraph] StateGraph 기반 파이프라인 워크플로우
# kosa-langchain-practice/langchain/api/sec09_multi_agent/ 참고
langgraph>=0.2.0

# [Sec05 - create_react_agent] LangChain 코어 및 Agent 생성
# kosa-langchain-practice/langchain/api/sec05_create_agent/ 참고
langchain>=0.2.0
langchain-core>=0.2.0

# [Sec05 - ChatOpenAI] OpenAI 모델 연동
# kosa-langchain-practice/langchain/api/sec05_create_agent/ 참고
langchain-openai>=0.1.0

# [Sec08 - RAG Agent] pgvector 저장소 연동 및 PDF 처리
# kosa-langchain-practice/langchain/api/sec08_rag/ 참고
langchain-community>=0.2.0
pypdf>=4.0.0
pgvector>=0.2.0
# [Hybrid Search] BM25 키워드 검색 + RRF 결합 (AGENT-SEARCH 명세서)
rank-bm25>=0.2.2

# [RAG-EMBED] OpenAI 임베딩 API 직접 호출 (text-embedding-3-large, dimensions=1536)
# RAG_EMBED_SPEC.md / EMBEDDING_MODEL_DECISION.md 참고
openai>=1.0.0

# [RAG-EMBED] 지수 백오프 재시도 (OpenAI API 호출 실패 시 최대 3회 재시도)
# RAG_EMBED_SPEC.md 구현 노트 참고
tenacity>=8.0.0

# ── 테스트 ──
# 테스트는 unittest 기반(IsolatedAsyncioTestCase)이라 `python -m unittest discover -s tests`로도 실행되지만,
# 표준 러너로 pytest를 사용한다(`python -m pytest tests`). 비동기 테스트가 unittest 자체 루프를 쓰므로
# pytest-asyncio는 불필요하다.
pytest>=8.0.0

# [TEST] sqlite+aiosqlite in-memory 엔진 (통합 테스트 test_team_isolation_http.py)
aiosqlite>=0.19.0

# ── AUTH (JWT 인증) ──
# [PROJECT-AUTH] JWT 토큰 생성/검증 (HS256)
python-jose[cryptography]>=3.3.0

# [PROJECT-AUTH] 비밀번호 bcrypt 해싱
bcrypt>=4.0.1
langgraph-checkpoint-postgres>=2.0.0
psycopg[binary]>=3.1.0
